<UserControl x:Class="Rythm.Client.View.ToolPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Rythm.Client.ViewModel;assembly=Rythm.Client.ViewModel"
             mc:Ignorable="d" 
             xmlns:interactivity="http://prismlibrary.com/"
             interactivity:ViewModelLocator.AutoWireViewModel="True" 
             d:DataContext="{d:DesignInstance viewModel:ToolPanelViewModel}">
    <Grid MinWidth="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

		<Grid Grid.Column="1" FlowDirection="RightToLeft"  Margin="5,0,0,0" HorizontalAlignment="Left" Width="250">
			<Button Style="{StaticResource BlackAndWhiteButton}" Content="Тема"  HorizontalAlignment="Left"/>
			<Button Style="{StaticResource BlackAndWhiteButton}" Content="Настройки" HorizontalAlignment="Center" Command="{Binding ChangeSettingsVisibilityCommand}"/>
			<Button Style="{StaticResource BlackAndWhiteButton}"  Content="События" HorizontalAlignment="Right" Command="{Binding DisplayingEventsVisibilityCommand}"/>
        </Grid>

        <Grid Grid.Column="0" HorizontalAlignment="Left" Margin="5,0,0,0" Visibility="{Binding SplitterVisibility}" Width="250">
			<Button  Style="{StaticResource BlackAndWhiteButton}" Content="Клиенты" Command="{Binding ShowClientsListCommand}" HorizontalAlignment="Left"/>
			<Button  Style="{StaticResource BlackAndWhiteButton}" Content="Сообщения" Command="{Binding ShowMessagesListCommand}" HorizontalAlignment="Center"/>
			<Button  Style="{StaticResource BlackAndWhiteButton}" Content="События" Command="{Binding ShowEventsListCommand}" HorizontalAlignment="Right"/>
        </Grid>
        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{Binding Login}" Visibility="{Binding LoginVisibility}" Foreground="DimGray" Width="Auto" FontSize="18" Margin="10,0,0,0" ></TextBlock>
    </Grid>

</UserControl>
