<UserControl x:Class="Rythm.Client.View.ToolPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Rythm.Client.ViewModel;assembly=Rythm.Client.ViewModel"
             mc:Ignorable="d" 
             xmlns:interactivity="http://prismlibrary.com/"
             interactivity:ViewModelLocator.AutoWireViewModel="True" 
             d:DataContext="{d:DesignInstance viewModel:ToolPanelViewModel}">
    <Grid MinWidth="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Orientation="Horizontal" FlowDirection="RightToLeft" Margin="1,1,5,1" HorizontalAlignment="Left">
            <Button  Width="70"  Height="30" Margin="0,5,5,5">
                <Button.Template>
                    <ControlTemplate>
                        <Grid >
                            <Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
                                <Label VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   Content="Тема" Foreground="DimGray"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button  Width="70"  Height="30" Margin="0,5,5,5" Command="{Binding ChangeSettingsVisibilityCommand}">
                <Button.Template>
                    <ControlTemplate>
                        <Grid >
                            <Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
                                <Label VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Content="Настройки" Foreground="DimGray"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
	        <Button  Width="70"  Height="30" Margin="0,5,5,5" Command="{Binding DisplayingEventsVisibilityCommand}">
		        <Button.Template>
			        <ControlTemplate>
				        <Grid >
					        <Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
						        <Label VerticalAlignment="Center"
						               HorizontalAlignment="Center"
						               Content="События" Foreground="DimGray"/>
					        </Border>
				        </Grid>
			        </ControlTemplate>
		        </Button.Template>
	        </Button>

		</StackPanel>

		<StackPanel Grid.Column="0" HorizontalAlignment="Left" Margin="5 0" Orientation="Horizontal" Visibility="{Binding SplitterVisibility}">
			<Button  Width="80"  Height="30" Margin="5,5,5,5" Command="{Binding ShowClientsListCommand}">
			    <Button.Template>
				    <ControlTemplate>
					    <Grid >
						    <Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
							    <Label VerticalAlignment="Center"
							           HorizontalAlignment="Center"
							           Content="Клиенты" Foreground="DimGray"/>
						    </Border>
					    </Grid>
				    </ControlTemplate>
			    </Button.Template>
		    </Button>
			<Button  Width="80"  Height="30" Margin="0,5,5,5" Command="{Binding ShowMessagesListCommand}">
			    <Button.Template>
				    <ControlTemplate>
					    <Grid >
						    <Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
							    <Label VerticalAlignment="Center"
							           HorizontalAlignment="Center"
							           Content="Сообщения" Foreground="DimGray"/>
						    </Border>
					    </Grid>
				    </ControlTemplate>
			    </Button.Template>
		    </Button>
			<Button  Width="80"  Height="30" Margin="0,5,5,5" Command="{Binding ShowEventsListCommand}">
				<Button.Template>
					<ControlTemplate>
						<Grid >
							<Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
								<Label VerticalAlignment="Center"
								       HorizontalAlignment="Center"
								       Content="События" Foreground="DimGray"/>
							</Border>
						</Grid>
					</ControlTemplate>
				</Button.Template>
			</Button>
		</StackPanel>
	    <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{Binding Login}" Visibility="{Binding LoginVisibility}" Foreground="DimGray" Width="Auto" FontSize="18" Margin="260,0,0,0" ></TextBlock>
    </Grid>

</UserControl>
