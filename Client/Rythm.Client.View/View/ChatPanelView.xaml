<UserControl x:Class="Rythm.Client.View.ChatPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Rythm.Client.ViewModel;assembly=Rythm.Client.ViewModel"
             mc:Ignorable="d"
             xmlns:interactivity="http://prismlibrary.com/"
            interactivity:ViewModelLocator.AutoWireViewModel="True" 
            d:DataContext="{d:DesignInstance viewModel:ChatPanelViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" ></RowDefinition>
            <RowDefinition Height="Auto" ></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0" ItemsSource="{Binding ReceivedMessagesList}" BorderThickness="0" Background="WhiteSmoke">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontSize="11"/>
                        <TextBlock Text="{Binding Text}" FontSize="14"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Time}" FontSize="8" TextAlignment="Right"/>
                            <TextBlock Text="{Binding ServerOkStatus}" FontSize="8" TextAlignment="Right"/>
                            <TextBlock Text="{Binding ClientOkStatus}" FontSize="8" TextAlignment="Right"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding OutgoingMessage, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,5,5" TextWrapping="Wrap" MaxHeight="100"/>
            <Button Grid.Column="1"  Margin="5" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Bottom"  MaxHeight="30" Command="{Binding SendCommand}">
                <Button.Template>
                    <ControlTemplate>
                        <Grid >
                            <Border BorderThickness="1" BorderBrush="LightGray"  CornerRadius="17"  >
                                <Label VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Content="Отправить" Foreground="DimGray"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</UserControl>
